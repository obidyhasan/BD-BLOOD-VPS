# Root production configuration is intentionally centralized in
# `.env.production.example`. Copy that file to `.env.production` and invoke
# Compose with `--env-file .env.production`.
#
# App-specific local-development templates remain in:
# - apps/api/.env.example
# - apps/web/.env.example
